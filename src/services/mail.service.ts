import {authValidator} from "../validator/auth.validator";
import * as nodemailer from "nodemailer"
export class MailService  {
    public static async sendMail (Email : string , confirmationLink : string){

    }
}